# 用户指南

## 1. 目标

本指南说明两类配置：

- 自动进食/繁殖的年龄逻辑与连续进食开关
- 通过数据包控制白名单与黑名单标签

## 2. 年龄逻辑与进食开关

当前逻辑：

- 成年动物：只有在 `age == 0`（无繁殖冷却）且 `canFallInLove()` 为真时才会自动进食。
- 幼崽：可由开关控制是否持续进食；默认开启，进食会加速成长。
- 幼崽长大后会自动转入成年规则，因此自然受到繁殖冷却约束。

对应 `SERVER` 配置键：

- `enableAutoFeed`：总开关
- `enableAdultContinuousFeeding`：成年自动进食开关（默认 `true`）
- `enableBabyContinuousFeeding`：幼崽持续进食开关（默认 `true`）

配置文件位置（实际由运行环境决定）：

1. 单机存档：`<Minecraft目录>/saves/<存档名>/serverconfig/natures_appetite-server.toml`
2. 专用服务器：`<服务器目录>/world/serverconfig/natures_appetite-server.toml`
3. 当前工程运行目录：`E:\Codes\MC\Nature's Appetite\.worktrees\codex-natures-appetite\runs\gameTestServer\world\serverconfig\natures_appetite-server.toml`

## 3. 标签数据包目录结构

对 `Minecraft 1.21.1`，`pack_format` 使用 `48`。

```text
<你的数据包目录>/
  pack.mcmeta
  data/
    natures_appetite/
      tags/
        entity_type/
          auto_feed_animals.json
          auto_feed_blacklist.json
```

## 4. 文件内容示例

### 4.1 `pack.mcmeta`

```json
{
  "pack": {
    "pack_format": 48,
    "description": "Nature's Appetite custom animal tags"
  }
}
```

### 4.2 `data/natures_appetite/tags/entity_type/auto_feed_animals.json`

> 白名单：只有在这个列表里的动物才会启用自动进食/自动繁殖逻辑（前提是未被黑名单排除）。

```json
{
  "replace": true,
  "values": [
    "minecraft:cow",
    "minecraft:sheep",
    "minecraft:pig",
    "minecraft:chicken"
  ]
}
```

### 4.3 `data/natures_appetite/tags/entity_type/auto_feed_blacklist.json`

> 黑名单：在白名单中也会被禁用。

```json
{
  "replace": true,
  "values": [
    "minecraft:pig"
  ]
}
```

逻辑优先级：

- 白名单决定“是否有资格启用”
- 黑名单最终否决（黑名单优先）

## 5. 数据包放置位置

可放在以下任一位置：

1. 单机存档  
`<Minecraft目录>/saves/<存档名>/datapacks/<你的包名>/...`

2. 专用服务器  
`<服务器目录>/world/datapacks/<你的包名>/...`

3. 当前项目的开发运行目录（本工程）  
`E:\Codes\MC\Nature's Appetite\.worktrees\codex-natures-appetite\runs\gameTestServer\world\datapacks\<你的包名>\...`

## 6. 使配置生效

进入游戏/服务器后执行：

```mcfunction
/reload
/datapack list
```

## 7. 备注

- 模组已内置默认白名单标签文件；你可以用数据包覆盖或扩展。
- 若想完全由你自己的数据包控制标签，建议将 `replace` 设为 `true`。
